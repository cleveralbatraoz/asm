# Конспект лекции по Assembly

## 1. Управляющие конструкции

### Пример `switch` в C
```c
switch (value) {
    case 1:
    case 2:   X;
    case 3:   { Y; break; }
    case 5:   Z;
    default:  D;
}
```

### Компиляция в ASM
```asm
; sub eax, 1
cmp eax, 5
ja  L4
jmp [eax * 4 + table]

L1: X
L2: Y
    jmp L5
L3: Z
L4: D
L5:

table dd L4, L1, L1, L2, L4, L3
        0   1   2   3   4   5
```
👉 `eax * 4`, так как адрес хранится в 4-байтных ячейках.

---

## 2. Организация функций

### Общая структура функции
```asm
push ebp
mov  ebp, esp
sub  esp, <locals>
...
mov  esp, ebp
pop  ebp
ret
```

### Роль EBP
- Используется как **base pointer** (опорная точка для локальных переменных и аргументов).
- Удобно для отладки и профилирования.
- Можно отключить (опция `omit frame pointer`) для экономии регистра.

### ENTER / LEAVE
| Инструкция | Эквивалент | Особенности |
|------------|------------|-------------|
| `enter`    | Сложная логика | Медленная, компиляторы не используют |
| `leave`    | `mov esp, ebp; pop ebp` | Может работать быстро или медленно в зависимости от CPU |

---

## 3. Локальная память и стек

### Выделение локальных переменных
- Стек растёт вниз.
- Для выделения используется `sub esp, N`.
- Рекомендуется выравнивать ESP на **кратные 4 или 8 байтам**.

### `alloca()`
- Аналог `malloc`, но выделяет **на стеке**.
- В C: `alloca(size)`
- Память освобождается автоматически при выходе из функции.
- Использование `alloca` вынуждает компилятор создавать стек-фрейм.

### Ограничения по стеку
- Обычно безопасно до **1 MB**.
- На Linux по умолчанию ~8 MB.
- Можно увеличить размер стека через настройки потока или флаги исполняемого файла.

---

## 4. Calling Conventions (x86, 32-bit)

### Сравнение соглашений
| Convention | Аргументы | Кто чистит стек | Особенности |
|------------|-----------|-----------------|-------------|
| **cdecl**  | В стеке (обратный порядок) | **Caller** | Поддерживает `vararg` |
| **stdcall**| В стеке (обратный порядок) | **Callee** | Нет `vararg` |
| **fastcall** | Первые аргументы в регистрах (`ecx`, `edx`), остальные в стеке | Разное (зависит от компилятора) | Не стандартизован |
| **thiscall** | `this` в `ecx`, остальные как в fastcall | **Callee** | Для методов классов |

### Примеры

#### cdecl
```asm
push e
push b
push a
call f
add  esp, 12 ; очистка
```

#### stdcall
```asm
push e
push b
push a
call f
; очистка внутри функции
ret  12
```

---

## 5. Возврат значений

| Тип возвращаемого значения | Где хранится |
|-----------------------------|--------------|
| `int` (4 байта)            | `eax` |
| `bool` (1 байт)            | `al` |
| `long long` (8 байта)       | `edx:eax` |

### Если результат не помещается в регистр
- Выделение памяти делает **caller**.
- В функцию передаётся указатель на буфер (как «нулевой аргумент»).
- Разные соглашения трактуют порядок (MSVC vs Unix).

---

## 6. Сохраняемые / несохраняемые регистры

| Категория | Регистры |
|-----------|----------|
| **Callee-saved** (должны сохраняться) | `ebx`, `ebp`, `esi`, `edi` |
| **Caller-saved** (могут портиться)   | `eax`, `ecx`, `edx` |

---

## 7. Практические советы

- Проектируйте API так, чтобы возвращаемое значение **умещалось в один регистр**.
- Для больших структур — используйте указатель на буфер.
- Для рекурсии или больших локальных массивов учитывайте лимит стека.
- Читайте: **Agner Fog – “Calling Conventions” (agner.org)**.

---

## 8. Дополнительно

### Выравнивание
- На x86 невыровненные обращения часто бесплатны, если не пересекают cache line.
- Лучше всё же выравнивать данные для предсказуемой производительности.

### Специализированные инструкции
- Современные CPU добавляют новые команды ради энергоэффективности.
- Пример: `CRC32` для контрольных сумм.
